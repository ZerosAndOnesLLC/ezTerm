{
  "name": "ezterm-workspace",
  "private": true,
  "description": "Workspace root — delegates UI scripts to ui/ so tauri-cli picks this folder as the project root.",
  "scripts": {
    "dev": "npm --prefix ui run dev",
    "build": "npm --prefix ui run build",
    "lint": "npm --prefix ui run lint",
    "typecheck": "npm --prefix ui run typecheck"
  }
}
